test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# BUG: credentials are hardcoded here instead of referencing Rails credentials or ENV.
# Anyone with repo access can read AWS keys.
amazon:
  service: S3
  access_key_id: AKIAIOSFODNN7EXAMPLE
  secret_access_key: wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
  region: us-east-1
  bucket: my-app-uploads
  # BUG: no ACL restriction configured — all uploads inherit the bucket's public-read policy.
